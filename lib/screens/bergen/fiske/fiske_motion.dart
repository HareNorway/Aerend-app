/// Fjordfiske's keyframe helpers live in the kit (`bergen_motion.dart`); the
/// Fiske names stay as aliases so the ledger's references still read.
library;

import '../kit/bergen_motion.dart';

export '../kit/bergen_motion.dart';

typedef FiskeLoop = BergenLoop;
typedef FiskeOnce = BergenOnce;
